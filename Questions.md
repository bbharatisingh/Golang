At every topic ask:

"What bottleneck does this solve?"

Example:

Cache ->
solves repeated DB reads

Load Balancer ->
solves server overload

CDN ->
solves global latency

Replication ->
solves DB read bottleneck

Sharding ->
solves DB size/write bottleneck

That chain is system design thinking.

Recommended Exact Learning Path
DB Indexing
Replication
Sharding
CAP Theorem
NoSQL
Kafka
Microservices
After These

Start full system design problems:

URL Shortener
Rate Limiter
Instagram Feed
WhatsApp
YouTube
Twitter Timeline
